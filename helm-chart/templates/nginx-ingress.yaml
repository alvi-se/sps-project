apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ngnix-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: imdb-web-service
                port:
                  number: 80
            
